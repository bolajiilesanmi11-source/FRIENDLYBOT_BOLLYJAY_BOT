services:
  - type: worker
    name: friendly-chat-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false # This prevents the token from being hardcoded here; you will add it via the dashboard.
